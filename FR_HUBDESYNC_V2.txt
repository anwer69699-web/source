-- Desync للاجهزه الضعيفة

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local StarterGui = game:GetService("StarterGui")
local Workspace = game:GetService("Workspace")


local function Notify(title, text, duration)
    duration = duration or 3
    pcall(function()
        StarterGui:SetCore("SendNotification", {
            Title = title,
            Text = text,
            Duration = duration,
            Icon = "rbxassetid://9079992154"
        })
    end)
end


if _G.FRHUB_DESYNC_LOADED then
    Notify("FR HUB", "السكربت محمل بالفعل!", 3)
    return
end
_G.FRHUB_DESYNC_LOADED = true


local ScriptSettings = {
    AutoDesync = false,
    Minimized = false
}


local function SaveSettings()
    pcall(function()
        local jsonString = HttpService:JSONEncode(ScriptSettings)
        writefile("FRHUBz_Config.json", jsonString)
    end)
end


local function LoadSettings()
    pcall(function()
        if isfile("FRHUBz_Config.json") then
            local jsonString = readfile("FRHUBz_Config.json")
            local loadedSettings = HttpService:JSONDecode(jsonString)
            for k, v in pairs(loadedSettings) do
                ScriptSettings[k] = v
            end
        end
    end)
end

LoadSettings()


local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "FRHUB_Desync_V9"
ScreenGui.ResetOnSpawn = false
ScreenGui.Parent = LocalPlayer:WaitForChild("PlayerGui")

local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 260, 0, 300)
MainFrame.Position = UDim2.new(0.5, -130, 0.4, -150)
MainFrame.BackgroundColor3 = Color3.fromRGB(25, 25, 25)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true
MainFrame.Parent = ScreenGui

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 10)
UICorner.Parent = MainFrame

local Header = Instance.new("Frame")
Header.Name = "Header"
Header.Size = UDim2.new(1, 0, 0, 35)
Header.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
Header.BorderSizePixel = 0
Header.Parent = MainFrame

local HeaderCorner = Instance.new("UICorner")
HeaderCorner.CornerRadius = UDim.new(0, 10)
HeaderCorner.Parent = Header

local Title = Instance.new("TextLabel")
Title.Size = UDim2.new(1, -50, 1, 0)
Title.Position = UDim2.new(0, 10, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "FR HUB |  Desync V1"
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.Font = Enum.Font.GothamBold
Title.TextXAlignment = Enum.TextXAlignment.Left
Title.Parent = Header


local dragging, dragInput, dragStart, startPos
local function update(input)
    local delta = input.Position - dragStart
    MainFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end
MainFrame.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = MainFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then dragging = false end
        end)
    end
end)
MainFrame.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then dragInput = input end
end)
UserInputService.InputChanged:Connect(function(input)
    if input == dragInput and dragging then update(input) end
end)


local DesyncEnabled = false
local Connection_RenderStepped = nil
local TargetServerPosition = nil 
local ActualServerPosition = nil 
local FakePosHighlight = nil
local FakePosLabel = nil
local ServerPosPart = nil
local CharacterAddedConnection = nil
local StatusLabel = nil
local RealPosLabel = nil

function FastRespawn()
    local char = game.Players.LocalPlayer.Character
    if not char then return end

    local hrp = char:FindFirstChild("HumanoidRootPart")
    local hum = char:FindFirstChildOfClass("Humanoid")

    if not hrp or not hum then return end

    
    hrp.Velocity = Vector3.new(0, 5000, 0)
    hrp.RotVelocity = Vector3.new(99999,99999,99999)

    
    task.delay(0.05, function()
        if hum then
            hum:ChangeState(Enum.HumanoidStateType.Dead)
        end
    end)
end


local function SetDesync(state)
    if state then
        if typeof(rnet) == "table" then
            if rnet.setdesync then pcall(function() rnet.setdesync(true) end) end
            if rnet.desync then pcall(function() rnet.desync(true) end) end
        end
        if typeof(delta) == "table" then
            if delta.raknet_desync then pcall(function() delta.raknet_desync(true) end) end
            if delta.desync then pcall(function() delta.desync(true) end) end
        end
    else
        if typeof(rnet) == "table" then
            if rnet.setdesync then pcall(function() rnet.setdesync(false) end) end
            if rnet.desync then pcall(function() rnet.desync(false) end) end
        end
        if typeof(delta) == "table" then
            if delta.raknet_desync then pcall(function() delta.raknet_desync(false) end) end
            if delta.desync then pcall(function() delta.desync(false) end) end
        end
    end
end


local function SendFakePosition(position)
    local cf = CFrame.new(position)
    if typeof(rnet) == "table" then
        if rnet.sendphysics then pcall(function() rnet.sendphysics(cf) end) end
        if rnet.sendposition then pcall(function() rnet.sendposition(position) end) end
    end
    if typeof(delta) == "table" then
        if delta.sendphysics then pcall(function() delta.sendphysics(cf) end) end
        if delta.sendposition then pcall(function() delta.sendposition(position) end) end
    end
end


local function GetActualServerPosition()
    
    if typeof(delta) == "table" and delta.get_server_position then
        local success, pos = pcall(delta.get_server_position)
        if success and pos and typeof(pos) == "Vector3" then
            return pos
        end
    end
    
    
    if typeof(rnet) == "table" and rnet.get_server_position then
        local success, pos = pcall(rnet.get_server_position)
        if success and pos and typeof(pos) == "Vector3" then
            return pos
        end
    end
    
    
    local char = LocalPlayer.Character
    if char then
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if hrp then
            local success, pos = pcall(function()
                return gethiddenproperty(hrp, "NetworkPosition") or 
                       gethiddenproperty(hrp, "ServerPosition") or
                       gethiddenproperty(hrp, "ReplicationPosition")
            end)
            if success and pos and typeof(pos) == "Vector3" then
                return pos
            end
        end
    end
    
    return nil 
end


local function CreateHighlight()
    if FakePosHighlight then FakePosHighlight:Destroy() end
    if FakePosLabel then FakePosLabel:Destroy() end
    if ServerPosPart then ServerPosPart:Destroy() end
    
    ServerPosPart = Instance.new("Part")
    ServerPosPart.Name = "ServerPosPart"
    ServerPosPart.Size = Vector3.new(4, 6, 4)
    ServerPosPart.Transparency = 1
    ServerPosPart.CanCollide = false
    ServerPosPart.Anchored = true
    ServerPosPart.Color = Color3.fromRGB(255, 0, 0)
    ServerPosPart.Material = Enum.Material.Neon
    ServerPosPart.Parent = workspace
    
    FakePosHighlight = Instance.new("BoxHandleAdornment")
    FakePosHighlight.Name = "ServerPosIndicator"
    FakePosHighlight.Size = Vector3.new(4, 6, 4)
    FakePosHighlight.Color3 = Color3.fromRGB(255, 0, 0)
    FakePosHighlight.Transparency = 0.3
    FakePosHighlight.ZIndex = 10
    FakePosHighlight.AlwaysOnTop = true
    FakePosHighlight.Parent = workspace
    FakePosHighlight.Visible = false
    
    FakePosLabel = Instance.new("BillboardGui")
    FakePosLabel.Name = "ServerPosLabel"
    FakePosLabel.Size = UDim2.new(0, 200, 0, 60)
    FakePosLabel.AlwaysOnTop = true
    FakePosLabel.Enabled = false
    FakePosLabel.Parent = workspace
    
    local TextLabel = Instance.new("TextLabel")
    TextLabel.Size = UDim2.new(1, 0, 1, 0)
    TextLabel.BackgroundTransparency = 1
    TextLabel.Text = "Test"
    TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    TextLabel.TextSize = 12
    TextLabel.Font = Enum.Font.SourceSansBold
    TextLabel.Parent = FakePosLabel
end

CreateHighlight()
local RubberPart, RubberHighlight, RubberLabel, RubberConn

function StartRB()
    if RubberConn then RubberConn:Disconnect() end

    if RubberPart then RubberPart:Destroy() end
    if RubberHighlight then RubberHighlight:Destroy() end
    if RubberLabel then RubberLabel:Destroy() end

    RubberPart = Instance.new("Part")
    RubberPart.Size = Vector3.new(4,6,4)
    RubberPart.Anchored = true
    RubberPart.CanCollide = false
    RubberPart.Transparency = 1
    RubberPart.Parent = workspace

    RubberHighlight = Instance.new("BoxHandleAdornment")
    RubberHighlight.Size = Vector3.new(4,6,4)
    RubberHighlight.Color3 = Color3.fromRGB(0,255,0)
    RubberHighlight.Transparency = 0.25
    RubberHighlight.AlwaysOnTop = true
    RubberHighlight.Adornee = RubberPart
    RubberHighlight.Parent = workspace

    RubberLabel = Instance.new("BillboardGui")
    RubberLabel.Size = UDim2.new(0,200,0,50)
    RubberLabel.AlwaysOnTop = true
    RubberLabel.Adornee = RubberPart
    RubberLabel.Parent = workspace

    local txt = Instance.new("TextLabel")
    txt.Size = UDim2.new(1,0,1,0)
    txt.BackgroundTransparency = 1
    txt.Text = "مكانك"
    txt.TextColor3 = Color3.fromRGB(0,255,0)
    txt.TextStrokeTransparency = 0
    txt.TextSize = 14
    txt.Font = Enum.Font.GothamBold
    txt.Parent = RubberLabel

    local lastPos = nil

    RubberConn = game:GetService("RunService").Heartbeat:Connect(function(dt)
        local char = game.Players.LocalPlayer.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if not hrp then return end

        local currentPos = hrp.Position
        local velocity = hrp.Velocity

        if lastPos then
            local delta = (currentPos - lastPos)
            local dist = delta.Magnitude

            
            local maxPossible = velocity.Magnitude * dt + 2

            
            local predicted = lastPos + (velocity * dt)
            local error = (currentPos - predicted).Magnitude

            
            if dist > 2 and error > 3 and dist > maxPossible then
                RubberPart.CFrame = CFrame.new(currentPos)
            end
        end

        lastPos = currentPos
    end)
end

function StopRB()
    if RubberConn then RubberConn:Disconnect() end
    if RubberHighlight then RubberHighlight:Destroy() end
    if RubberPart then RubberPart:Destroy() end
    if RubberLabel then RubberLabel:Destroy() end
end


local ToggleBtn = Instance.new("TextButton")
ToggleBtn.Name = "ToggleBtn"
ToggleBtn.Size = UDim2.new(0.9, 0, 0, 45)
ToggleBtn.Position = UDim2.new(0.05, 0, 0.15, 0)
ToggleBtn.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
ToggleBtn.Text = "تفعيل Desync"
ToggleBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
ToggleBtn.TextSize = 18
ToggleBtn.Font = Enum.Font.GothamBold
ToggleBtn.AutoButtonColor = false
ToggleBtn.Parent = MainFrame

local BtnCorner = Instance.new("UICorner")
BtnCorner.CornerRadius = UDim.new(0, 8)
BtnCorner.Parent = ToggleBtn


StatusLabel = Instance.new("TextLabel")
StatusLabel.Name = "StatusLabel"
StatusLabel.Size = UDim2.new(0.9, 0, 0, 20)
StatusLabel.Position = UDim2.new(0.05, 0, 0.32, 0)
StatusLabel.BackgroundTransparency = 1
StatusLabel.Text = "الحالة: متوقف"
StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
StatusLabel.TextSize = 12
StatusLabel.Font = Enum.Font.GothamBold
StatusLabel.Parent = MainFrame


RealPosLabel = Instance.new("TextLabel")
RealPosLabel.Name = "RealPosLabel"
RealPosLabel.Size = UDim2.new(0.9, 0, 0, 30)
RealPosLabel.Position = UDim2.new(0.05, 0, 0.38, 0)
RealPosLabel.BackgroundTransparency = 1
RealPosLabel.Text = " خلي سرعتك في ديول السرعه العاديه = 40 سرعه الستيل = 15 "
RealPosLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
RealPosLabel.TextSize = 10
RealPosLabel.Font = Enum.Font.Gotham
RealPosLabel.TextWrapped = true
RealPosLabel.Parent = MainFrame


local function OnCharacterAdded(newChar)
    if not DesyncEnabled then return end
    
    local hrp = newChar:WaitForChild("HumanoidRootPart", 2)
    if not hrp then return end
    
    SetDesync(true)
    
    task.spawn(function()
        while DesyncEnabled and newChar.Parent do
            if TargetServerPosition then
                SendFakePosition(TargetServerPosition)
            end
            task.wait(0.02)
        end
    end)
end

local function ToggleDesync()
DesyncEnabled = not DesyncEnabled
if DesyncEnabled then
    StartRB()
        TweenService:Create(ToggleBtn, TweenInfo.new(0.3), {BackgroundColor3 = Color3.fromRGB(0, 170, 127), TextColor3 = Color3.fromRGB(255, 255, 255)}):Play()
        ToggleBtn.Text = "Desync: مفعل"
        StatusLabel.Text = "جاري المحاولة..."
        StatusLabel.TextColor3 = Color3.fromRGB(255, 255, 100)
        
        
        local char = LocalPlayer.Character
        local hrp = char and char:FindFirstChild("HumanoidRootPart")
        if hrp then
            TargetServerPosition = hrp.Position
        end
        
        SetDesync(true)
        FastRespawn()
        Notify("FR HUB", "تم تفعيل الDesync!", 2)
        
        if not FakePosHighlight or not FakePosHighlight.Parent then
            CreateHighlight()
        end
        
        
        if TargetServerPosition and ServerPosPart then
            ServerPosPart.CFrame = CFrame.new(TargetServerPosition)
            ServerPosPart.Transparency = 1
            FakePosHighlight.CFrame = CFrame.new(TargetServerPosition)
            FakePosHighlight.Visible = false
            FakePosLabel.Adornee = ServerPosPart
            FakePosLabel.Enabled = false
        end
        
        
        Connection_RenderStepped = RunService.RenderStepped:Connect(function()
            local char = LocalPlayer.Character
            local hrp = char and char:FindFirstChild("HumanoidRootPart")
            
            if hrp and TargetServerPosition then
                
                SendFakePosition(TargetServerPosition)
                
                
                ActualServerPosition = GetActualServerPosition()
                
                local realPos = hrp.Position
                local distance = (realPos - TargetServerPosition).Magnitude
                
                if ActualServerPosition then
                    
                    local actualDistance = (ActualServerPosition - TargetServerPosition).Magnitude
                    RealPosLabel.Text = "السيرفر " .. math.floor(ActualServerPosition.X) .. ", " .. math.floor(ActualServerPosition.Z) ..
                                       "\nالفرق: " .. math.floor(actualDistance) .. "m"
                    
                    if actualDistance < 5 then
                        
                        StatusLabel.Text = "ناجح! (" .. math.floor(actualDistance) .. "m)"
                        StatusLabel.TextColor3 = Color3.fromRGB(0, 255, 100)
                        FakePosHighlight.Color3 = Color3.fromRGB(0, 255, 0)
                        FakePosLabel.TextLabel.Text = "so dont care abt ts js test"
                    else
                        
                        StatusLabel.Text = "فشل! (" .. math.floor(actualDistance) .. "m)"
                        StatusLabel.TextColor3 = Color3.fromRGB(255, 50, 50)
                        FakePosHighlight.Color3 = Color3.fromRGB(255, 0, 0)
                        FakePosLabel.TextLabel.Text = "TEST"
                    end
                else
                    
                    RealPosLabel.Text = " خلي سرعتك في ديول السرعه العاديه = 40 سرعه الستيل = 15 "
                    
                    if distance > 10 then
                        StatusLabel.Text = "شغال (" .. math.floor(distance) .. "m)"
                        StatusLabel.TextColor3 = Color3.fromRGB(100, 255, 100)
                    else
                        StatusLabel.Text = "شغال😆 (" .. math.floor(distance) .. "m)"
                        StatusLabel.TextColor3 = Color3.fromRGB(255, 200, 0)
                    end
                end
            end
        end)
        
        CharacterAddedConnection = LocalPlayer.CharacterAdded:Connect(OnCharacterAdded)
    else
    StopRB()
        TweenService:Create(ToggleBtn, TweenInfo.new(0.3), {BackgroundColor3 = Color3.fromRGB(45, 45, 45), TextColor3 = Color3.fromRGB(200, 200, 200)}):Play()
        ToggleBtn.Text = "تفعيل Desync"
        StatusLabel.Text = "الحالة: متوقف"
        StatusLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
        RealPosLabel.Text = "سويلي لايك و فولو"
        SetDesync(false)
        if Connection_RenderStepped then Connection_RenderStepped:Disconnect() end
        if CharacterAddedConnection then CharacterAddedConnection:Disconnect() end
        if FakePosHighlight then 
            FakePosHighlight.Visible = false 
            FakePosHighlight.Color3 = Color3.fromRGB(255, 0, 0)
        end
        if FakePosLabel then 
            FakePosLabel.Enabled = false 
            FakePosLabel.TextLabel.Text = "test"
        end
        if ServerPosPart then ServerPosPart.Transparency = 1 end
        TargetServerPosition = nil
        ActualServerPosition = nil
        Notify("FR HUB", "تم إيقاف الDesync!", 2)
    end
end


ToggleBtn.MouseButton1Click:Connect(ToggleDesync)
    


local SaveSettingsBtn = Instance.new("TextButton")
SaveSettingsBtn.Size = UDim2.new(0.9, 0, 0, 30)
SaveSettingsBtn.Position = UDim2.new(0.05, 0, 0.5, 0)
SaveSettingsBtn.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
SaveSettingsBtn.Text = "تشغيل Desyncتلقاءي"
SaveSettingsBtn.TextColor3 = Color3.fromRGB(200, 200, 200)
SaveSettingsBtn.TextSize = 14
SaveSettingsBtn.Font = Enum.Font.GothamBold
SaveSettingsBtn.Parent = MainFrame
local SaveBtnCorner = Instance.new("UICorner")
SaveBtnCorner.CornerRadius = UDim.new(0, 8)
SaveBtnCorner.Parent = SaveSettingsBtn

SaveSettingsBtn.MouseButton1Click:Connect(function()
    ScriptSettings.AutoDesync = DesyncEnabled
    SaveSettings()
    Notify("FR HUB", "تم بنجاج", 2)
end)


local Credits = Instance.new("TextLabel")
Credits.Size = UDim2.new(1, 0, 0, 25)
Credits.Position = UDim2.new(0, 0, 1, -25)
Credits.BackgroundTransparency = 1
Credits.Text = "© FR HUB - DesyncScript"
Credits.TextColor3 = Color3.fromRGB(100, 100, 100)
Credits.TextSize = 12
Credits.Font = Enum.Font.Gotham
Credits.Parent = MainFrame


local MinimizeBtn = Instance.new("TextButton")
MinimizeBtn.Name = "MinimizeBtn"
MinimizeBtn.Size = UDim2.new(0, 30, 0, 30)
MinimizeBtn.Position = UDim2.new(1, -35, 0, 2)
MinimizeBtn.BackgroundTransparency = 1
MinimizeBtn.Text = "-"
MinimizeBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
MinimizeBtn.TextSize = 24
MinimizeBtn.Font = Enum.Font.GothamBold
MinimizeBtn.Parent = Header

local UIElements = {ToggleBtn, StatusLabel, RealPosLabel, SaveSettingsBtn, Credits}

MinimizeBtn.MouseButton1Click:Connect(function()
    ScriptSettings.Minimized = not ScriptSettings.Minimized
    
    if ScriptSettings.Minimized then
        MinimizeBtn.Text = "+"
        TweenService:Create(MainFrame, TweenInfo.new(0.3), {
            Size = UDim2.new(0, 260, 0, 35)
        }):Play()
        for _, element in ipairs(UIElements) do
            element.Visible = false
        end
    else
        MinimizeBtn.Text = "-"
        TweenService:Create(MainFrame, TweenInfo.new(0.3), {
            Size = UDim2.new(0, 260, 0, 300)
        }):Play()
        for _, element in ipairs(UIElements) do
            element.Visible = true
        end
    end
end)

task.delay(1, function()
    if ScriptSettings.AutoDesync and not DesyncEnabled then
        ToggleDesync()
    end
end)
Notify("FR HUB", "تم التحميل بنجاح!", 4)
